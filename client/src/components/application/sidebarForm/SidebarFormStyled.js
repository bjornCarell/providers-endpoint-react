import styled from 'styled-components';
import { Form } from '../../library/form/Form';

export const SidebarFormStyled = styled(Form)`
  background: rgba(1, 63, 69, 0.97);
  box-shadow: -3px 9px 17px -2px rgba(0, 0, 0, 0.3);
  position: fixed;
  z-index: 5;
`;
